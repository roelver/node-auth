# Server side authentication module

This is the completed server side authentication project form Stephen Grider's Advanced React and Redux course on Udemy.
It uses Node, Passport (JWT and Local) and MongoDB for storing the users.

### API

`/signup` - signup with email and password, returning a JWT token

`/signin` - signin  with email and password, returning a JWT token

other routes can be setup for authentication

